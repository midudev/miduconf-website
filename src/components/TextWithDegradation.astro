---
const COLORS = {
  orange: ['from-miduconf-orange-200', 'to-miduconf-orange-100'],
  purple: ['from-miduconf-purple-200', 'to-miduconf-purple-100'],
}

const [from, to] = COLORS[Astro.props.color]
---
<span class={`bg-clip-text bg-gradient-to-r ${from} ${to} text-transparent tracking-tigh`}><slot /></span>